import { createTheme } from "@mui/material/styles";

export const cursiveHeaders = createTheme({
  typography: {
    fontFamily: ["The Nautigal", "cursive"].join(","),
  },
});

export const mainTopic = createTheme({
  typography: {
    fontFamily: ["Playfair Display", "serif"].join(","),
  },
});
export const underPhoto = createTheme({
  typography: {
    fontFamily: ["Square Peg", "cursive"].join(","),
  },
});

export const mainTabs = createTheme({
  typography: {
    fontFamily: ["Raleway", "sans-serif"].join(","),
  },
});
